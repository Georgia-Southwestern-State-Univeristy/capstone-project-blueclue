# ML Inference Service
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0.0

# ML / Data
scikit-learn>=1.4.0
numpy>=1.26.0
scipy>=1.12.0
joblib>=1.3.0
pandas>=2.2.0

# Explainability
shap>=0.45.0

# NLP
spacy>=3.8.0

# ─── LLM + RAG ──────────────────────────────────────────────────────────────
# Cloud LLM (OpenAI GPT-3.5/4, embeddings)
openai>=1.30.0

# Free local embedding fallback (sentence-transformers/all-MiniLM-L6-v2)
# Install only if you need local embeddings (adds ~500MB to image)
sentence-transformers>=2.7.0
torch>=2.2.0,<3.0.0

# pgvector Python adapter (for constructing vector literals if needed)
pgvector>=0.3.0
# ────────────────────────────────────────────────────────────────────────────

# Utilities
python-dotenv==1.0.1
requests==2.32.0
psycopg2-binary>=2.9.0

# Legacy (kept for backward compat scripts)
Flask==3.1.0
flask-cors==5.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
